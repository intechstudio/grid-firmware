name: Publish firmware

on:
  push:
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-firmware:
    name: Build firmware
    uses: ./.github/workflows/build-and-push-firmware.yml

  publish-stable:
    runs-on: ubuntu-latest
    name: Publish stable release
    needs: build-firmware
    if: needs.build-firmware.outputs.publish_stable
    steps:
      - name: boop the beep
        run: echo "the beep has been booped"
